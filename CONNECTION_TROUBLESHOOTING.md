# 🔧 IPL Auction Connection Troubleshooting Guide\n\n## Quick Fix Solutions\n\n### 🚀 Automatic Fix (Recommended)\n1. **Run the connection fix utility:**\n   ```\n   Double-click: fix-connection.bat\n   ```\n   This will automatically:\n   - Check if server is running\n   - Configure Windows Firewall\n   - Display your IP address for mobile devices\n   - Test the connection\n\n### 🖥️ Manual Server Start\n1. **Enhanced server startup:**\n   ```\n   Double-click: start-server.bat\n   ```\n   The new script includes:\n   - Port availability checking\n   - Dependency installation\n   - Network information display\n   - Error handling\n\n## Common Connection Issues & Solutions\n\n### ❌ Issue 1: \"Connection Failed\" or \"Server Not Running\"\n\n**Solutions:**\n1. **Check if server is running:**\n   - Open Command Prompt\n   - Run: `netstat -an | findstr :3000`\n   - If nothing appears, server is not running\n\n2. **Start the server:**\n   - Double-click `start-server.bat`\n   - Wait for \"Server running on port 3000\" message\n\n3. **Check for port conflicts:**\n   - Close other applications using port 3000\n   - Restart your computer if needed\n\n### ❌ Issue 2: Mobile Devices Can't Connect\n\n**Solutions:**\n1. **Use correct IP address:**\n   - Find your computer's IP: Run `ipconfig` in Command Prompt\n   - Look for \"IPv4 Address\" (usually 192.168.x.x)\n   - Use: `http://YOUR_IP:3000` (not localhost)\n\n2. **Check WiFi network:**\n   - Ensure all devices are on the SAME WiFi network\n   - Avoid guest networks or mobile hotspots\n\n3. **Disable Windows Firewall temporarily:**\n   - Go to Windows Security → Firewall & network protection\n   - Turn off firewall for private networks\n   - Test connection, then re-enable firewall\n\n### ❌ Issue 3: \"Reconnecting...\" Loop\n\n**Solutions:**\n1. **Refresh the page:**\n   - Press Ctrl+F5 to hard refresh\n   - Clear browser cache if needed\n\n2. **Check network stability:**\n   - Ensure stable WiFi connection\n   - Move closer to WiFi router\n\n3. **Restart server:**\n   - Close server (Ctrl+C in command prompt)\n   - Run `start-server.bat` again\n\n### ❌ Issue 4: Room Creation/Joining Fails\n\n**Solutions:**\n1. **Wait for connection:**\n   - Ensure \"✅ Connected to server\" status\n   - Don't click buttons until connected\n\n2. **Check Room ID:**\n   - Room IDs are case-insensitive\n   - Use exact Room ID shared by host\n\n3. **Try different browser:**\n   - Chrome and Edge work best\n   - Enable JavaScript\n\n## 🔍 Built-in Diagnostics\n\n### Network Diagnostics Tool\n- Click the \"🔍 Network Diagnostics\" button (bottom-right corner)\n- Automatically runs when connection fails multiple times\n- Shows detailed connection status and troubleshooting steps\n\n### Server Status Indicators\n- **🔄 Connecting to server...** - Attempting connection\n- **✅ Connected to server** - Ready for multiplayer\n- **❌ Connection failed** - Check server and network\n- **❌ Server not running** - Start the server first\n\n## 📱 Mobile-Specific Issues\n\n### iOS Safari Issues\n1. **Enable JavaScript:**\n   - Settings → Safari → Advanced → JavaScript (ON)\n\n2. **Clear Safari cache:**\n   - Settings → Safari → Clear History and Website Data\n\n### Android Chrome Issues\n1. **Allow mixed content:**\n   - Chrome → Settings → Site Settings → Insecure content (Allow)\n\n2. **Disable data saver:**\n   - Chrome → Settings → Lite mode (OFF)\n\n## 🛠️ Advanced Troubleshooting\n\n### Check Node.js Installation\n```bash\nnode --version\nnpm --version\n```\nIf not installed, download from: https://nodejs.org/\n\n### Manual Dependency Installation\n```bash\ncd Backend\nnpm install\n```\n\n### Check Firewall Rules\n```bash\nnetsh advfirewall firewall show rule name=\"Node.js\"\n```\n\n### Add Firewall Exception\n```bash\nnetsh advfirewall firewall add rule name=\"Node.js\" dir=in action=allow program=\"C:\\Program Files\\nodejs\\node.exe\" enable=yes\n```\n\n## 🆘 Still Having Issues?\n\n### Step-by-Step Checklist\n- [ ] Node.js is installed\n- [ ] Server is running (start-server.bat)\n- [ ] Port 3000 is not blocked\n- [ ] All devices on same WiFi\n- [ ] Using correct IP address\n- [ ] Browser JavaScript enabled\n- [ ] Windows Firewall configured\n\n### Contact Information\nIf issues persist:\n1. Run network diagnostics and note the errors\n2. Check browser console for error messages (F12)\n3. Try different devices/browsers\n4. Restart router and computer\n\n## 📋 Quick Reference\n\n| Problem | Quick Fix |\n|---------|----------|\n| Server not running | Run `start-server.bat` |\n| Mobile can't connect | Use IP address, not localhost |\n| Connection timeout | Check firewall and WiFi |\n| Room join fails | Wait for \"Connected\" status |\n| Frequent disconnects | Check network stability |\n\n---\n\n**Remember:** The most common issue is using `localhost:3000` on mobile devices. Always use your computer's IP address (e.g., `192.168.1.100:3000`) for mobile connections!